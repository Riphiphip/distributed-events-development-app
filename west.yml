manifest:
  version: "0.12"
  remotes:
      # nRF Connect SDK base URL.
    - name: ncs
      url-base: https://github.com/nrfconnect

  projects:
    - name: nrf
      remote: ncs
      repo-path: sdk-nrf
      revision: v2.1.0
      import: true

  self:
    path: distributed_events_poc
