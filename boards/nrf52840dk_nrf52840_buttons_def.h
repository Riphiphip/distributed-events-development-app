#include <caf/gpio_pins.h>

static const struct gpio_pin row[] = {
    {.port = 0, .pin = 11},
    {.port = 0, .pin = 12},
    {.port = 0, .pin = 24},
    {.port = 0, .pin = 25},
};

static const struct gpio_pin col[] = {};